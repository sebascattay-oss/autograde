Create a public GitHub repository called autograde. The Python CLI tool must inspect the valid target repository and report whether it meets a series of conditions detailed below:

1. The project is a git repository
2. That the "main" branch exists
3. That the "feature" branch exists in remote
4. That the "file1.txt" file exists in main



